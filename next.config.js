/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['littleladywitching.co.uk', 'res.cloudinary.com'],
  },
};

module.exports = nextConfig;